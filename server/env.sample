# ============================================================
# RAMBLAA SERVER ENVIRONMENT VARIABLES
# ============================================================
# Copy this file to .env and fill in your values
# For Railway: Set these in the Variables tab per environment
# ============================================================

# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o-mini

# ============================================================
# TWILIO CONFIGURATION
# ============================================================
# These can be shared across environments (same Twilio account)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# ⚠️ IMPORTANT: This should be DIFFERENT per environment!
# Each staging environment needs its own WhatsApp number
# with its webhook pointing to that environment's URL.
#
# Example setup:
#   staging-danyon:   whatsapp:+15551111111 → backend-staging-danyon.up.railway.app
#   staging:          whatsapp:+15552222222 → backend-staging.up.railway.app
#   staging-harrison: whatsapp:+15553333333 → backend-staging-harrison.up.railway.app
#   production:       whatsapp:+15554444444 → backend.up.railway.app
#
TWILIO_WHATSAPP_NUMBER=whatsapp:+1XXXXXXXXXX

# ============================================================
# DATABASE
# ============================================================
# Railway provides this automatically via internal networking
# Format: postgresql://user:password@host:5432/database
DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway

# ============================================================
# SERVER
# ============================================================
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5174

# ============================================================
# JWT AUTHENTICATION
# ============================================================
# Generate secure secrets for production!
# Example: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-change-in-production-minimum-32-chars
JWT_REFRESH_SECRET=your-refresh-secret-change-in-production-minimum-32-chars

# ============================================================
# EMAIL SERVICE (SendGrid)
# ============================================================
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your-sendgrid-api-key
EMAIL_FROM_ADDRESS=noreply@ramblaa.com
EMAIL_FROM_NAME=Ramblaa
# Frontend URL for email links (must match deployed frontend)
FRONTEND_URL=http://localhost:5174

# ============================================================
# EXTERNAL WEBHOOK (optional)
# ============================================================
WEBHOOK_URL=
WEBHOOK_API_KEY=
ACCOUNT_ID=1
